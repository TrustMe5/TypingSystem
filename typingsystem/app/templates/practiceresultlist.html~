{% extends "base.html" %}
{% block title%}日常打字情况{%endblock%}
{% block page_content %}
<script>
window.onload=function(){
{% if not writer %}
    document.getElementById('iflogin').innerHTML="<a href='/login'>登陆</a> ";
    self.location='/';
    {%else%}
    document.getElementById('iflogin').innerHTML="<a href='/homepage/user={{writer}}'>当前用户:{{writer}}</a>"
    {%endif%}
}
</script>
<center><h1> 日常打字情况 </h1></center>
<b>Go To Volume</b>
{%for i in range(totalpagenum)%}
<a href='/practiceresultlist/page={{loop.index}}'>[{{loop.index}}]</a>
{%endfor%}
</br>
<table class='table table-bordered table-condensed' >
    <tbody>
         <tr class='info'>
        <td>ID</td>
        <td>用户名</td>
        <td>文章名字</td>
        <td>打字速度</td>
        <td>正确率</td>
        <td>打字数目</td>
        <td>日期</td>
        </tr>
        {% for list in result%}
        <tr>
            <td>{{list.id}}</td>
            <td>{{list.username}}</td>
            <td><a href='/articlelist/{{list.articlename}}'>{{list.articlename}}</a></td>
            <td>{{list.write_speed}}</td>
            <td>{{list.right_rate}}</td>
            <td>{{list.writenum}}</td>
            <td>{{list.submitTime}}</td>
            </tr>
            {% endfor %}
    </tbody>
    </table>
    
{% endblock %}
